// Proposal detail - three panes:
//   1. the parliamentary item (excerpt + link to the official report)
//   2. the affected clause in the current knowledge base + its graph neighbourhood
//   3. the suggested redline, with Approve / Reject
// TODO(M4)
export default function ProposalDetail() {
  return <main className="p-8">Proposal detail (M4)</main>;
}
