"""SQLite engine + session helpers. Vault markdown is the source of truth for
document *content*; this database holds state, relations and workflow."""
# TODO(M1): create_engine(f"sqlite:///{settings.db_path}"), init_db(), get_session()
